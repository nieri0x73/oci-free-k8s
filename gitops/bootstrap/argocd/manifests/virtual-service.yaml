apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: argocd
  namespace: argocd
spec:
  hosts:
    - gitops.nieri0x73.com
  gateways:
    - argocd
  http:
    - route:
        - destination:
            host: argocd-server
            port:
              number: 80
