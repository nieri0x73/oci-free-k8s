apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: n8n
  namespace: apps
spec:
  hosts:
    - automation.nieri0x73.com
  gateways:
    - n8n
  http:
    - route:
        - destination:
            host: n8n
            port:
              number: 5678
