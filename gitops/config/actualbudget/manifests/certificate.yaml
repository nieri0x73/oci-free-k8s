apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: actualbudget-tls
  namespace: istio-system
spec:
  secretName: actualbudget-tls
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
    - finance.nieri0x73.com
