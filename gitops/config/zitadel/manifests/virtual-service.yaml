apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: zitadel
  namespace: security
spec:
  hosts:
    - iam.nieri0x73.com
  gateways:
    - zitadel
  http:
    - route:
        - destination:
            host: zitadel
            port:
              number: 8080
