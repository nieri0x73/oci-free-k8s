apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: evolution-api-tls
  namespace: istio-system
spec:
  secretName: evolution-api-tls
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
    - evolution.nieri0x73.com
