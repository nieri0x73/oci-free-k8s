apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls
  namespace: istio-system
spec:
  secretName: keycloak-tls
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
  dnsNames:
    - keycloak.NIP_IO_PLACEHOLDER
    - keycloak.DNS_PLACEHOLDER
