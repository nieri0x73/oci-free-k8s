apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls
  namespace: istio-system
spec:
  secretName: keycloak-tls
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
    - keycloak.NIP_IO_PLACEHOLDER
    - iam.nieri0x73.com
